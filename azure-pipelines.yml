# =============================================================================
# Azure Pipelines - Task API (Spring Boot + Azure App Service)
# =============================================================================
# Mejoras incluidas:
# - Cache de Maven para builds más rápidos
# - Tests con reportes JUnit y cobertura JaCoCo
# - Stage de Staging antes de Producción
# - Verificación de app después de cada deploy (compatible con Plan Free)
# - Smoke Tests de API
# - Limpieza de artefactos antiguos
# - DownloadPipelineArtifact@2 (más estable)
# =============================================================================

trigger:
  branches:
    include:
      - main
      - develop
      - feature/*
  paths:
    exclude:
      - README.md
      - docs/*
      - '*.md'
      - '.gitignore'

pr:
  branches:
    include:
      - main
      - develop

# =============================================================================
# POOL DE AGENTES
# =============================================================================
# Opción 1: Agente de Microsoft (recomendado si no tienes agente propio)
pool:
  vmImage: 'ubuntu-latest'

# Opción 2: Tu agente self-hosted (descomentar si lo tienes configurado)
# pool:
#   name: 'my-agent'

# =============================================================================
# VARIABLES
# =============================================================================
variables:
  # Azure Service Connection
  # ⚠️ IMPORTANTE: Debes crear esta conexión en Azure DevOps:
  #    Project Settings → Service connections → New → Azure Resource Manager
  #    Nombre: azure-task-api (o cambia el valor abajo por el nombre de tu conexión)
  azureServiceConnection: 'azure-task-api'
  
  # Resource Group
  resourceGroup: 'rg-task-api'
  
  # App Services
  appServiceStaging: 'task-api-emmanuel-staging'
  appServiceProduction: 'task-api-emmanuel'
  
  # URLs para verificaciones
  stagingUrl: 'https://$(appServiceStaging).azurewebsites.net'
  productionUrl: 'https://$(appServiceProduction).azurewebsites.net'
  
  # Maven
  MAVEN_CACHE_FOLDER: $(Pipeline.Workspace)/.m2/repository
  MAVEN_OPTS: '-Dmaven.repo.local=$(MAVEN_CACHE_FOLDER)'
  
  # Build
  artifactName: 'task-api-artifact'
  
  # Flags
  runTests: 'true'
  deployToStaging: 'true'

# =============================================================================
# STAGES
# =============================================================================
stages:
  # ===========================================================================
  # STAGE 1: BUILD & TEST
  # ===========================================================================
  - stage: Build
    displayName: '🔨 Build & Test'
    jobs:
      - job: BuildAndTest
        displayName: 'Compilar y Probar'
        steps:
          # -------------------------------------------------------------------
          # Checkout
          # -------------------------------------------------------------------
          - checkout: self
            displayName: '📥 Checkout código'
            fetchDepth: '0'

          # -------------------------------------------------------------------
          # Cache de Maven
          # -------------------------------------------------------------------
          - task: Cache@2
            displayName: '📦 Cache Maven Dependencies'
            inputs:
              key: 'maven | "$(Agent.OS)" | **/pom.xml'
              restoreKeys: |
                maven | "$(Agent.OS)"
                maven
              path: $(MAVEN_CACHE_FOLDER)

          # -------------------------------------------------------------------
          # Instalar Java 17 (necesario para agentes hosted de Microsoft)
          # -------------------------------------------------------------------
          - task: JavaToolInstaller@0
            displayName: '☕ Instalar Java 17'
            inputs:
              versionSpec: '17'
              jdkArchitectureOption: 'x64'
              jdkSourceOption: 'PreInstalled'

          # -------------------------------------------------------------------
          # Verificar herramientas
          # -------------------------------------------------------------------
          - script: |
              echo "=========================================="
              echo "         VERIFICANDO HERRAMIENTAS         "
              echo "=========================================="
              echo ""
              echo "=== Java Version ==="
              java -version
              echo ""
              echo "=== Maven Version ==="
              mvn -version
              echo ""
              echo "=== Variables de entorno ==="
              echo "JAVA_HOME: $JAVA_HOME"
              echo "Build.SourceBranch: $(Build.SourceBranch)"
              echo "Build.BuildId: $(Build.BuildId)"
            displayName: '🔍 Verificar Java y Maven'

          # -------------------------------------------------------------------
          # Compilar y ejecutar tests
          # -------------------------------------------------------------------
          - task: Maven@4
            displayName: '🧪 Maven Build & Test'
            inputs:
              mavenPomFile: 'pom.xml'
              goals: 'clean verify'
              options: '$(MAVEN_OPTS) -B -Dspring.profiles.active=test'
              javaHomeOption: 'JDKVersion'
              jdkVersionOption: '1.17'
              publishJUnitResults: true
              testResultsFiles: '**/surefire-reports/TEST-*.xml'
              codeCoverageToolOption: 'JaCoCo'
              codeCoverageClassFilesDirectories: 'target/classes'
              codeCoverageSourceDirectories: 'src/main/java'
            condition: eq(variables['runTests'], 'true')

          # -------------------------------------------------------------------
          # Build sin tests (fallback si tests están deshabilitados)
          # -------------------------------------------------------------------
          - task: Maven@4
            displayName: '📦 Maven Build (sin tests)'
            inputs:
              mavenPomFile: 'pom.xml'
              goals: 'clean package'
              options: '$(MAVEN_OPTS) -B -DskipTests'
              javaHomeOption: 'JDKVersion'
              jdkVersionOption: '1.17'
            condition: ne(variables['runTests'], 'true')

          # -------------------------------------------------------------------
          # Nota: La cobertura JaCoCo se publica automáticamente por Maven@4
          # con codeCoverageToolOption: 'JaCoCo' (no necesita tarea adicional)
          # -------------------------------------------------------------------

          # -------------------------------------------------------------------
          # Verificar que el JAR se generó
          # -------------------------------------------------------------------
          - script: |
              echo "=== Verificando artefactos generados ==="
              ls -la target/
              echo ""
              echo "=== JAR generado ==="
              ls -la target/*.jar
            displayName: '🔎 Verificar JAR generado'

          # -------------------------------------------------------------------
          # Copiar archivos para deploy
          # -------------------------------------------------------------------
          - task: CopyFiles@2
            displayName: '📋 Copiar JAR al staging'
            inputs:
              SourceFolder: '$(System.DefaultWorkingDirectory)/target'
              Contents: '*.jar'
              TargetFolder: '$(Build.ArtifactStagingDirectory)'

          # -------------------------------------------------------------------
          # Copiar archivos de configuración adicionales
          # -------------------------------------------------------------------
          - task: CopyFiles@2
            displayName: '📋 Copiar archivos de configuración'
            inputs:
              SourceFolder: '$(System.DefaultWorkingDirectory)'
              Contents: |
                scripts/**
                pom.xml
              TargetFolder: '$(Build.ArtifactStagingDirectory)/config'
            continueOnError: true

          # -------------------------------------------------------------------
          # Publicar artefacto
          # -------------------------------------------------------------------
          - task: PublishBuildArtifacts@1
            displayName: '📦 Publicar Artefacto'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: '$(artifactName)'
              publishLocation: 'Container'

          # -------------------------------------------------------------------
          # Resumen del build
          # -------------------------------------------------------------------
          - script: |
              echo "=========================================="
              echo "           BUILD COMPLETADO               "
              echo "=========================================="
              echo "Branch: $(Build.SourceBranch)"
              echo "Build ID: $(Build.BuildId)"
              echo "Commit: $(Build.SourceVersion)"
              echo "Artefacto: $(artifactName)"
              echo "=========================================="
            displayName: '✅ Resumen del Build'

  # ===========================================================================
  # STAGE 2: DEPLOY TO STAGING
  # ===========================================================================
  - stage: DeployStaging
    displayName: '🚀 Deploy Staging'
    dependsOn: Build
    condition: |
      and(
        succeeded(),
        eq(variables['deployToStaging'], 'true'),
        ne(variables['Build.Reason'], 'PullRequest')
      )
    variables:
      environmentName: 'staging'
    jobs:
      - deployment: DeployToStaging
        displayName: 'Deploy a Staging'
        environment: 'staging'
        strategy:
          runOnce:
            deploy:
              steps:
                # -----------------------------------------------------------
                # Descargar artefacto (versión mejorada)
                # -----------------------------------------------------------
                - task: DownloadPipelineArtifact@2
                  displayName: '📥 Descargar artefacto'
                  inputs:
                    buildType: 'current'
                    artifactName: '$(artifactName)'
                    targetPath: '$(Pipeline.Workspace)/$(artifactName)'

                # -----------------------------------------------------------
                # Verificar artefactos descargados
                # -----------------------------------------------------------
                - script: |
                    echo "=== Verificando contenido del artefacto ==="
                    find $(Pipeline.Workspace) -name "*.jar" -type f
                    echo ""
                    echo "=== Estructura de directorios ==="
                    ls -la $(Pipeline.Workspace)/$(artifactName)/
                  displayName: '🔎 Verificar artefactos descargados'

                # -----------------------------------------------------------
                # Deploy a Azure App Service Staging
                # -----------------------------------------------------------
                - task: AzureWebApp@1
                  displayName: '☁️ Deploy a App Service Staging'
                  inputs:
                    azureSubscription: '$(azureServiceConnection)'
                    appType: 'webAppLinux'
                    appName: '$(appServiceStaging)'
                    package: '$(Pipeline.Workspace)/$(artifactName)/*.jar'
                    runtimeStack: 'JAVA|17-java17'
                    startUpCommand: ''

                # -----------------------------------------------------------
                # Esperar que la aplicación inicie
                # -----------------------------------------------------------
                - script: |
                    echo "=== Esperando que la aplicación inicie (60s) ==="
                    sleep 60
                  displayName: '⏳ Esperar inicio de aplicación'

                # -----------------------------------------------------------
                # Verificar que la app responde (sin Actuator - Plan Free)
                # -----------------------------------------------------------
                - script: |
                    echo "=== Verificando App Staging ==="
                    echo "URL: $(stagingUrl)"
                    
                    for i in 1 2 3 4 5; do
                      echo "Intento $i de 5..."
                      HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $(stagingUrl)/api/tasks || echo "000")
                      
                      if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "403" ]; then
                        echo "✅ App respondiendo! HTTP $HTTP_CODE"
                        exit 0
                      else
                        echo "⚠️ HTTP $HTTP_CODE - Reintentando en 15s..."
                        sleep 15
                      fi
                    done
                    
                    echo "⚠️ App no respondió después de 5 intentos (continuando...)"
                    exit 0
                  displayName: '🔍 Verificar App Staging'
                  continueOnError: 'true'

                # -----------------------------------------------------------
                # Verificar Swagger
                # -----------------------------------------------------------
                - script: |
                    echo "=== Verificando Swagger UI ==="
                    HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $(stagingUrl)/swagger-ui.html || echo "000")
                    
                    if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "302" ]; then
                      echo "✅ Swagger UI disponible: $(stagingUrl)/swagger-ui.html"
                    else
                      echo "⚠️ Swagger UI retornó HTTP $HTTP_CODE"
                    fi
                  displayName: '📚 Verificar Swagger UI'
                  continueOnError: 'true'

  # ===========================================================================
  # STAGE 3: SMOKE TESTS (Opcional)
  # ===========================================================================
  - stage: SmokeTests
    displayName: '🧪 Smoke Tests'
    dependsOn: DeployStaging
    condition: succeeded()
    jobs:
      - job: RunSmokeTests
        displayName: 'Ejecutar Smoke Tests'
        steps:
          - script: |
              echo "=========================================="
              echo "         EJECUTANDO SMOKE TESTS           "
              echo "=========================================="
              
              API_URL="$(stagingUrl)/api"
              
              # Test 1: GET /api/tasks
              echo ""
              echo "=== Test 1: GET /api/tasks ==="
              HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $API_URL/tasks)
              if [ "$HTTP_CODE" = "200" ]; then
                echo "✅ GET /api/tasks - HTTP $HTTP_CODE"
              else
                echo "⚠️ GET /api/tasks - HTTP $HTTP_CODE"
              fi
              
              # Test 2: GET /api/tasks/stats
              echo ""
              echo "=== Test 2: GET /api/tasks/stats ==="
              HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $API_URL/tasks/stats)
              if [ "$HTTP_CODE" = "200" ]; then
                echo "✅ GET /api/tasks/stats - HTTP $HTTP_CODE"
                curl -s $API_URL/tasks/stats | head -20
              else
                echo "⚠️ GET /api/tasks/stats - HTTP $HTTP_CODE"
              fi
              
              # Test 3: POST /api/tasks (crear tarea de prueba)
              echo ""
              echo "=== Test 3: POST /api/tasks ==="
              RESPONSE=$(curl -s -X POST $API_URL/tasks \
                -H "Content-Type: application/json" \
                -d '{"title":"Test Pipeline $(Build.BuildId)","description":"Tarea creada por pipeline","status":"PENDING"}')
              echo "Response: $RESPONSE"
              
              echo ""
              echo "=========================================="
              echo "       SMOKE TESTS COMPLETADOS            "
              echo "=========================================="
            displayName: '🔥 Smoke Tests API'
            continueOnError: 'true'

  # ===========================================================================
  # STAGE 4: DEPLOY TO PRODUCTION
  # ===========================================================================
  - stage: DeployProduction
    displayName: '🏭 Deploy Production'
    dependsOn: SmokeTests
    condition: |
      and(
        succeeded(),
        eq(variables['Build.SourceBranch'], 'refs/heads/main')
      )
    variables:
      environmentName: 'production'
    jobs:
      - deployment: DeployToProduction
        displayName: 'Deploy a Production'
        environment: 'production'  # Requiere aprobación manual en Azure DevOps
        strategy:
          runOnce:
            deploy:
              steps:
                # -----------------------------------------------------------
                # Descargar artefacto (versión mejorada)
                # -----------------------------------------------------------
                - task: DownloadPipelineArtifact@2
                  displayName: '📥 Descargar artefacto'
                  inputs:
                    buildType: 'current'
                    artifactName: '$(artifactName)'
                    targetPath: '$(Pipeline.Workspace)/$(artifactName)'

                # -----------------------------------------------------------
                # Verificar artefactos descargados
                # -----------------------------------------------------------
                - script: |
                    echo "=== Verificando contenido del artefacto ==="
                    find $(Pipeline.Workspace) -name "*.jar" -type f
                    echo ""
                    echo "=== Estructura de directorios ==="
                    ls -la $(Pipeline.Workspace)/$(artifactName)/
                  displayName: '🔎 Verificar artefactos descargados'

                # -----------------------------------------------------------
                # Deploy a Azure App Service Production
                # -----------------------------------------------------------
                - task: AzureWebApp@1
                  displayName: '☁️ Deploy a App Service Production'
                  inputs:
                    azureSubscription: '$(azureServiceConnection)'
                    appType: 'webAppLinux'
                    appName: '$(appServiceProduction)'
                    package: '$(Pipeline.Workspace)/$(artifactName)/*.jar'
                    runtimeStack: 'JAVA|17-java17'

                # -----------------------------------------------------------
                # Esperar inicio
                # -----------------------------------------------------------
                - script: |
                    echo "=== Esperando que la aplicación inicie (90s) ==="
                    sleep 90
                  displayName: '⏳ Esperar inicio de aplicación'

                # -----------------------------------------------------------
                # Verificar que la app responde (sin Actuator - Plan Free)
                # -----------------------------------------------------------
                - script: |
                    echo "=== Verificando App Production ==="
                    
                    for i in 1 2 3 4 5 6; do
                      echo "Intento $i de 6..."
                      HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $(productionUrl)/api/tasks || echo "000")
                      
                      if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "403" ]; then
                        echo "✅ App respondiendo! HTTP $HTTP_CODE"
                        exit 0
                      else
                        echo "⚠️ HTTP $HTTP_CODE - Reintentando en 20s..."
                        sleep 20
                      fi
                    done
                    
                    echo "⚠️ App no respondió como esperado (verificar logs)"
                    exit 0
                  displayName: '🔍 Verificar App Production'
                  continueOnError: 'true'

                # -----------------------------------------------------------
                # Resumen final
                # -----------------------------------------------------------
                - script: |
                    echo "=========================================="
                    echo "     🎉 DEPLOY COMPLETADO EXITOSAMENTE    "
                    echo "=========================================="
                    echo ""
                    echo "📍 URLs de Producción:"
                    echo "   API:     $(productionUrl)/api/tasks"
                    echo "   Swagger: $(productionUrl)/swagger-ui.html"
                    echo ""
                    echo "📋 Información del Build:"
                    echo "   Build ID: $(Build.BuildId)"
                    echo "   Branch:   $(Build.SourceBranch)"
                    echo "   Commit:   $(Build.SourceVersion)"
                    echo "=========================================="
                  displayName: '✅ Resumen Deploy Production'

  # ===========================================================================
  # STAGE 5: CLEANUP (Opcional - Solo en main)
  # ===========================================================================
  - stage: Cleanup
    displayName: '🧹 Cleanup'
    dependsOn: DeployProduction
    condition: |
      and(
        succeeded(),
        eq(variables['Build.SourceBranch'], 'refs/heads/main')
      )
    jobs:
      - job: CleanupOldArtifacts
        displayName: 'Limpiar recursos antiguos'
        steps:
          - script: |
              echo "=== Limpieza de recursos ==="
              echo "Este stage puede incluir:"
              echo "- Eliminar builds antiguos"
              echo "- Limpiar logs"
              echo "- Purgar caché"
              echo ""
              echo "Por ahora, solo mostramos información del deploy completado."
              echo ""
              echo "Build $(Build.BuildId) completado exitosamente."
            displayName: '🗑️ Cleanup Info'